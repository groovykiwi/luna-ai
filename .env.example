# Copy this file to .env and fill in the real values.
OPENROUTER_API_KEY=

# Used by local pnpm runs (pnpm chat / pnpm worker).
BOT_PATH=./bots/maya

# Used by docker compose to mount the host bot folder into /bot.
BOT_HOST_PATH=./bots/maya
